name: Build App

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[build]"

      - name: Generate icons
        run: python scripts/make_icon.py

      - name: Build macOS .app
        run: python -m PyInstaller bangladesh-serosurveillance-luminex-qc.spec --clean -y

      - name: Codesign (ad-hoc)
        run: codesign --force --deep --sign - "dist/Bangladesh Serosurveillance Luminex QC.app"

      - name: Create zip archive
        run: |
          cd dist
          zip -r "Bangladesh-Serosurveillance-Luminex-QC-macOS.zip" "Bangladesh Serosurveillance Luminex QC.app"

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: Bangladesh-Serosurveillance-Luminex-QC-macOS
          path: dist/Bangladesh-Serosurveillance-Luminex-QC-macOS.zip

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[build]"

      - name: Generate icons
        run: python scripts/make_icon.py

      - name: Build Windows exe
        run: python -m PyInstaller bangladesh-serosurveillance-luminex-qc-win.spec --clean -y

      - name: Create zip archive
        run: Compress-Archive -Path "dist/Bangladesh-Serosurveillance-Luminex-QC" -DestinationPath "dist/Bangladesh-Serosurveillance-Luminex-QC-Windows.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: Bangladesh-Serosurveillance-Luminex-QC-Windows
          path: dist/Bangladesh-Serosurveillance-Luminex-QC-Windows.zip

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/Bangladesh-Serosurveillance-Luminex-QC-macOS/Bangladesh-Serosurveillance-Luminex-QC-macOS.zip
            artifacts/Bangladesh-Serosurveillance-Luminex-QC-Windows/Bangladesh-Serosurveillance-Luminex-QC-Windows.zip
